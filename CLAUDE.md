# オペレーションハブ（kintai-checker）— Claude Code 向けガイド

リポジトリ運用の基本方針（製品名・コミットの刻み方・共有ワークツリーの注意・jinjer API の制約）は
[AGENTS.md](AGENTS.md) に記載している。本ファイルはそれに加えて、Claude Code で実装するときの
開発フローを定める。

## 開発フロー

実装依頼を受けたら、次の順で進める。

1. **実装前に仕様と合格条件を整理する。** 何をどう変えるか、何を満たせば完了かを箇条書きにし、ユーザーと認識を合わせてから着手する。
2. **実装する。** AGENTS.md のとおり、作業前にブランチを切り、区切りごとにコミットする。
3. **既存テストと追加テストを実行する。** `python -m pytest` で既存テスト全体を通し、今回の変更に対する追加テストも通す。
4. **system-reviewer へ変更差分、仕様、テスト結果を渡す。** ユーザー階層の `~/.claude/agents/system-reviewer.md`（読み取り専用の共通レビュー担当）に、1 で整理した仕様と合格条件・`git diff` の出力（または変更ファイル一覧）・3 のテスト出力・前提メモ（`.claude/review-notes/_common.md` と対象モードのメモ）のパスを渡してレビューさせる。
5. **指摘を鵜呑みにせず、根拠を確認して採否を判断する。** 指摘ごとに示された `ファイル:行` を実際に読み、再現する入力があるかを確かめる。根拠が無い指摘は理由を添えて見送る。納得できない指摘は反論の根拠を付けて system-reviewer に返し、往復で決着させる（下記「レビュー担当との協議」）。
6. **妥当な指摘だけを修正する。** 見送った指摘とその理由も記録に残す。
7. **修正後にテストを再実行する。** 既存テストと追加テストの両方を通し直す。
8. **レビューと修正は最大 2 回までとする。** 2 回目のレビューでも重大な問題が残る場合は、それ以上自力で回さずユーザーに状況を報告して判断を仰ぐ。
9. **最後に変更点、テスト結果、残存リスク、人間の確認事項を報告する。** 変更点は「どういう意図で、こう変えた／こう戻した」の形で書く。system-reviewer の指摘と改善提案について、採用・見送り・実装後に戻した、のどれにしたかと理由を一覧にする。「リリース前に人間が確認すべき事項」はそのまま引き継いで書く。

## レビュー担当との協議

system-reviewer は「重大／軽微な問題」と「改善提案（仕様外）」を分けて返す。扱いは次のとおり。

- **問題の指摘**は、根拠を確認して妥当なら修正する。反論があれば根拠を付けて返し、往復は最大 2 回。決着しないものは人間の確認事項に回す。
- **改善提案**は、仕様の範囲内で戻しやすいものなら協議のうえ実装してよい。実装したら再テストし、悪化していれば戻す。戻した場合もその事実と理由を報告する。
- **仕様を変える提案、不可逆な処理（外部投入・削除・マスタ変更・送信）に触れる提案、共有マスタや設定 CSV の形式を変える提案**は、実装せずに報告で提案として挙げ、ユーザーの判断を待つ。
- 画面の変更は、静的な確認（コンソールエラー・id の参照）までを Claude 側で行い、既存ボタンを含む実クリックは人間の確認事項として必ず報告に載せる。

## モード単位の棚卸し

差分が無くても、モード全体を system-reviewer に読ませて穴を洗い出せる。呼ぶときは「棚卸し」と明記し、対象モードの前提メモを渡す。結果の「人間が確認すべき事項」と「改善提案」はそのモードのメモの宿題欄へ追記する。新しいシステムをハブに統合したときは、統合直後に一度この棚卸しを回す。
